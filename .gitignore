# dados brutos — regenerados a cada retreino, nao precisam de versionamento
data/

# lixo de SO / editores
.DS_Store
__MACOSX/
__pycache__/
*.pyc

# config local do Claude Code, nao e do projeto
.claude/

# script local com o token real do Telegram (nunca commitar)
start_bot.bat
.env

# versoes antigas / ferramentas de debug manual, fora do repo
alert.py
btc_alert-.py
diagnose.py
predict_live.py

# saida de treino que so serve de referencia local, regenerada toda semana
oos_predictions.parquet
